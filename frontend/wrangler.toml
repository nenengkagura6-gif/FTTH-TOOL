# ==================================================
# Cloudflare Pages Configuration
# ==================================================

name = "ftth-tool"
compatibility_date = "2024-01-01"

# Build settings
[build]
command = "npm run build"
output_directory = "out"

# Environment variables (production)
[env.production]
name = "ftth-tool"

# Environment variables (preview)
[env.preview]
name = "ftth-tool-preview"
